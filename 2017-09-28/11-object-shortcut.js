// If variables (or arguments)
// have same names as properties
const x =  1 /  2;
const y = 29 / 63;

// Put together at right of =
// with object literal shortcut
const point = {
  x,
  y,
};
console.log('object literal shortcut:', point);
